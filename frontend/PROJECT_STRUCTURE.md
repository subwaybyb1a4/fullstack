# 프로젝트 폴더 구조 가이드

이 문서는 React Native + Expo 프로젝트의 폴더 구조를 설명합니다.

## 📁 주요 폴더 구조

```
frontend/
├── app/                    # 🎯 핵심: 화면(스크린) 파일들이 들어있는 폴더
├── assets/                 # 이미지, 폰트 등 정적 리소스
├── components/             # 재사용 가능한 UI 컴포넌트
├── constants/              # 상수값 (테마, 색상 등)
├── data/                   # 더미 데이터, 목업 데이터
├── hooks/                  # 커스텀 React 훅
├── utils/                  # 유틸리티 함수들
├── scripts/                # 빌드/배포 관련 스크립트
└── [설정 파일들]
```

---

## 📂 각 폴더 상세 설명

### 1. `app/` - 화면(스크린) 폴더 ⭐ **가장 중요**

**Expo Router의 파일 기반 라우팅 시스템**을 사용합니다. 파일 경로가 곧 URL 경로가 됩니다.

```
app/
├── .tsx          _layout    # 루트 레이아웃 (모든 화면의 부모)
├── (tabs)/                  # 탭 네비게이션 그룹 (괄호는 URL에 포함 안됨)
│   ├── _layout.tsx          # 탭 레이아웃
│   ├── index.tsx            # 홈 화면 (/)
│   ├── favorites.tsx        # 즐겨찾기 화면 (/favorites)
│   └── settings.tsx         # 설정 화면 (/settings)
├── search.tsx               # 검색 화면 (/search)
├── results.tsx               # 검색 결과 화면 (/results)
├── modal.tsx                # 모달 화면 (/modal)
├── detail/
│   └── [id].tsx             # 동적 라우트 - 상세 화면 (/detail/123)
└── tracking/
    └── [id].tsx             # 동적 라우트 - 추적 화면 (/tracking/456)
```

**특징:**

- `_layout.tsx`: 네비게이션 구조를 정의 (Stack, Tabs 등)
- `(tabs)`: 괄호로 감싸면 URL에 포함되지 않는 그룹
- `[id].tsx`: 동적 파라미터 (예: `/detail/123`에서 `123`이 `id`)
- `index.tsx`: 해당 폴더의 기본 화면

**예시:**

- `app/index.tsx` → `/` (홈)
- `app/search.tsx` → `/search`
- `app/detail/[id].tsx` → `/detail/123`, `/detail/456` 등

---

### 2. `assets/` - 정적 리소스

이미지, 폰트, 아이콘 등 앱에서 사용하는 정적 파일들

```
assets/
└── images/
    ├── icon.png                    # 앱 아이콘
    ├── splash-icon.png             # 스플래시 스크린 이미지
    ├── android-icon-*.png          # 안드로이드 아이콘들
    └── ...
```

**사용 방법:**

```tsx
import { Image } from 'expo-image';
<Image source={require('../assets/images/icon.png')} />
```

---

### 3. `components/` - 재사용 가능한 UI 컴포넌트

여러 화면에서 공통으로 사용하는 컴포넌트들

```
components/
├── ui/                      # UI 라이브러리 컴포넌트 (버튼, 카드 등)
│   ├── collapsible.tsx
│   └── ...
├── themed-text.tsx          # 테마 적용된 텍스트
├── themed-view.tsx          # 테마 적용된 뷰
└── ...
```

**사용 예시:**

```tsx
// app/index.tsx에서
import { ThemedText } from '../components/themed-text';
```

---

### 4. `constants/` - 상수값

앱 전역에서 사용하는 상수들 (색상, 테마, 설정값 등)

```
constants/
└── theme.ts                 # 색상, 폰트 등 테마 관련 상수
```

**예시:**

```tsx
// constants/theme.ts
export const Colors = {
  light: { text: '#11181C', ... },
  dark: { text: '#ECEDEE', ... }
};

// 사용
import { Colors } from '../constants/theme';
```

---

### 5. `data/` - 더미/목업 데이터

개발 중 테스트용 데이터나 API 응답 목업 데이터

```
data/
└── mockData.ts              # 더미 데이터
```

---

### 6. `hooks/` - 커스텀 React 훅

재사용 가능한 로직을 훅으로 만든 것들

```
hooks/
├── use-color-scheme.ts      # 다크모드/라이트모드 감지
└── use-theme-color.ts       # 테마 색상 가져오기
```

**사용 예시:**

```tsx
import { useColorScheme } from '../hooks/use-color-scheme';
const colorScheme = useColorScheme(); // 'light' | 'dark'
```

---

### 7. `utils/` - 유틸리티 함수

범용적으로 사용하는 헬퍼 함수들

```
utils/
└── storage.ts               # AsyncStorage 관련 함수 (로컬 저장소)
```

**예시:**

```tsx
// utils/storage.ts
export async function saveFavorites(data) { ... }
export async function getFavorites() { ... }
```

---

### 8. `scripts/` - 빌드/배포 스크립트

개발 도구 스크립트들

```
scripts/
└── reset-project.js         # 프로젝트 초기화 스크립트
```

---

### 9. `guidelines/` - 프로젝트 가이드라인

프로젝트 개발 규칙, 디자인 가이드라인 등 문서

---

## ⚙️ 설정 파일들

### `app.json` - Expo 앱 설정

앱 이름, 아이콘, 스플래시 스크린, 플러그인 등 설정

### `package.json` - 의존성 관리

설치된 패키지와 스크립트 명령어

### `tsconfig.json` - TypeScript 설정

TypeScript 컴파일러 옵션

### `babel.config.js` - Babel 설정

JavaScript/TypeScript 변환 설정 (Expo, NativeWind 등)

### `tailwind.config.js` - Tailwind CSS 설정

NativeWind (React Native용 Tailwind) 설정

### `eslint.config.js` - ESLint 설정

코드 린팅 규칙

---

## 🚀 주요 명령어

```bash
# 개발 서버 시작
npm start
# 또는
expo start

# 안드로이드 에뮬레이터에서 실행
npm run android

# iOS 시뮬레이터에서 실행 (Mac만 가능)
npm run ios

# 코드 린팅
npm run lint
```

---

## 📝 작업 시 주의사항

1. **새 화면 추가**: `app/` 폴더에 파일 추가하면 자동으로 라우트 생성됨
2. **컴포넌트 재사용**: `components/`에 공통 컴포넌트 만들기
3. **상수 관리**: `constants/`에 하드코딩된 값들 모아두기
4. **스타일링**: NativeWind (Tailwind) 사용 중이므로 클래스 기반 스타일 가능

---

## 🔗 참고 자료

- [Expo Router 공식 문서](https://docs.expo.dev/router/introduction/)
- [React Native 공식 문서](https://reactnative.dev/)
- [NativeWind 문서](https://www.nativewind.dev/)
